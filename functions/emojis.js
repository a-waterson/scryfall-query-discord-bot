// let str = "{U} {B}";

// let map = {
//     "{U}": "Blue",
// };

// let regex = new RegExp(Object.keys(map).join("|"), "gi");
// str = str.replace(regex, function(matched) {
//     return map[matched];
// });